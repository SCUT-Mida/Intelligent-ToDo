/**
 * Placeholder: RepoNavSettings component.
 *
 * This is a TEMPORARY stub to unblock the build. The parallel frontend agent
 * will replace this with the full implementation (templates list, config forms,
 * AI memory controls, search UI).
 *
 * The component receives no props — it reads/writes config via IPC directly.
 */

export default function RepoNavSettings(): JSX.Element {
  return (
    <div style={{ padding: '16px', color: '#666' }}>
      仓库导航设置将在下一版本中完善
    </div>
  )
}
